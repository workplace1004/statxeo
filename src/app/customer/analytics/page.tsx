import {CustomerAnalyticsPage} from "../../../views/customer/analytics-page";

export default function Page() {
  return (
    <CustomerAnalyticsPage
      channels={[]}
      conversions={[]}
      devices={[]}
      leads={[]}
      traffic={[]}
    />
  );
}
